import { Library } from '../views';

export default function HomePage(): React.ReactElement {
  return (
    <div style={{ marginInline: 'inherit', maxWidth: 'inherit', paddingBlockStart: '60px' }}>
      <Library />
    </div>
  );
};
